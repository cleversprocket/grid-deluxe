{
  "name": "grid-deluxe",
  "version": "1.0.0",
  "description": "A CSS flex grid with more than just columns and rows.",
  "repository": {
    "type": "git",
    "url": "git@github.com:cleversprocket/grid-deluxe.git"
  },
  "scripts": {
    "prepublish": "npm run build",
    "build": "gulp",
    "clean": "gulp clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "CSS",
    "grid",
    "flex"
  ],
  "author": "Matt Morrow <matt@cleversprocket.com>",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^7.1.6",
    "cssnano": "^3.10.0",
    "del": "^2.0.2",
    "gulp": "^3.9.0",
    "gulp-postcss": "^7.0.0",
    "gulp-rename": "^1.2.2",
    "gulp-sass": "^3.1.0"
  }
}
